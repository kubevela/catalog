name: model-training
version: 1.0.0
description: Enable training for models

tags:
- ai
- training
- alpha

dependencies:
  - name: fluxcd

deployTo:
  control_plane: true
  runtime_cluster: true

system:
  kubernetes: <1.22
