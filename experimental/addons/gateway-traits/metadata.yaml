name: gateway-traits
version: v0.0.1
description: traits(HttpRoute, HttpsRoute, tcpRoute) for gateway

tags:
  - Gateway
  - Trait

deployTo:
  controlPlane: true
  runtimeCluster: true

system:
  vela: ">=v1.2.4"
