# This can be used for pending some faulty addons temporarily.
# Please fix them as soon as possible!

ocm-gateway-manager-addon
model-serving
flink-kubernetes-operator
vela-core-shard-manager
grafana
loki
prometheus-server
# rollout and traefik both pull charts from charts.kubevela.net/{core,community},
# whose SSL certificate has expired. Fluxcd source-controller correctly refuses
# the fetch. Re-enable once the upstream KubeVela chart-host certificate is
# renewed (or the charts are republished to a working registry).
rollout
traefik
